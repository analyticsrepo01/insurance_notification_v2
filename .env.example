# Google Cloud Project Configuration
# Required for ADK and Vertex AI
GOOGLE_CLOUD_PROJECT=your-project-id

# Email Configuration (Optional - for sending real emails)
# Leave SENDER_PASSWORD empty to run in demo mode (emails printed to console)
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-gmail-app-password

# SMTP Server Configuration (Default: Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Gmail App Password Setup:
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Visit: https://myaccount.google.com/apppasswords
# 3. Generate an app password for "Mail"
# 4. Use the 16-character password above (no spaces)

# Note: If SENDER_PASSWORD is empty, the agent runs in DEMO MODE
# and prints email notifications to console instead of sending them.
